name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "roboai/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: roboai/package-lock.json

      - name: Install system dependencies for Sharp and OG image generation
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            fonts-dejavu-core \
            fonts-liberation \
            libvips-dev

      - name: Install dependencies
        working-directory: roboai
        run: npm ci

      - name: Run type checking
        working-directory: roboai
        run: npm run typecheck

      - name: Build site
        working-directory: roboai
        env:
          GA4_MEASUREMENT_ID: ${{ secrets.GA4_MEASUREMENT_ID }}
          BOOK_ASSISTANT_API_URL: ${{ secrets.BOOK_ASSISTANT_API_URL }}
          REACT_APP_AUTH_SERVICE_URL: ${{ secrets.REACT_APP_AUTH_SERVICE_URL }}
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: roboai/build

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    if: needs.build.result == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
