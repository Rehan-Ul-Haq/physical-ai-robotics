# =============================================================================
# Docusaurus Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values for local development.
# For production, set these in your CI/CD environment (e.g., GitHub Actions secrets).

# -----------------------------------------------------------------------------
# PanaversityFS Plugin Configuration
# -----------------------------------------------------------------------------
# Enable the plugin to fetch content from PanaversityFS MCP server at build time.
#
# When DISABLED (default):
#   - Docusaurus reads from docs/ folder (local sample content)
#   - No server connection needed
#   - Good for UI/technical development without server setup
#
# When ENABLED:
#   - Plugin fetches content from MCP server and writes to docsfs/
#   - Docusaurus reads from docsfs/ folder
#   - Requires running MCP server (local or production)
#
PANAVERSITY_PLUGIN_ENABLED=false

# URL of the PanaversityFS MCP server.
# - Local development: http://localhost:8000/mcp
# - Production: Your hosted server URL (e.g., https://panaversity-fs.example.com/mcp)
PANAVERSITY_SERVER_URL=http://localhost:8000/mcp

# -----------------------------------------------------------------------------
# Google Analytics 4 (Optional)
# -----------------------------------------------------------------------------
# Your GA4 measurement ID for analytics tracking.
# Leave empty to disable analytics.
# GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Book Assistant API (Optional)
# -----------------------------------------------------------------------------
# URL of the Book Assistant backend API for the ChatWidget.
# - Local development: http://localhost:8001
# - Production: Your hosted API URL
# If not set, defaults to http://localhost:8001
# BOOK_ASSISTANT_API_URL=http://localhost:8001

# -----------------------------------------------------------------------------
# Better-Auth Service (Authentication)
# -----------------------------------------------------------------------------
# URL of the Better-Auth service for user authentication.
# - Local development: http://localhost:8002
# - Production: Your hosted auth service URL
# Required for sign-in, sign-up, OAuth, and session management.
REACT_APP_AUTH_SERVICE_URL=http://localhost:8002

# Environment mode for auth client logging
REACT_APP_ENV=development
